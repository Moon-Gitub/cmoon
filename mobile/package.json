{
  "name": "cmoon-mobile",
  "version": "1.0.0",
  "description": "POSMoon Android — caja offline con escaneo",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:apk": "npx cap sync android && cd android && ./gradlew assembleDebug && mkdir -p ../dist && cp -f app/build/outputs/apk/debug/app-debug.apk ../dist/POSMoon-1.0.0-debug.apk",
    "build:apk:release": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/network": "^6.0.3",
    "@capacitor-mlkit/barcode-scanning": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
